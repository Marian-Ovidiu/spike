/**
 * Exchange order direction (aggressive side of a trade).
 */
export type OrderSide = "buy" | "sell";

/**
 * Economic exposure of an open derivatives position (linear perpetual convention).
 */
export type PositionSide = "long" | "short";
