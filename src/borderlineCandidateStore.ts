import { BorderlineCandidateManager } from "./borderlineCandidate.js";

/**
 * Thin alias to make intent explicit at call sites:
 * this object is strategy state storage for borderline candidates.
 */
export { BorderlineCandidateManager as BorderlineCandidateStore };
